[targets.pico]

[[targets.pico.serial]]
name = "console"
device = "/dev/cu.usbserial-AAA"
baud = 115200

[[targets.pico.serial]]
name = "bmc"
device = "/dev/ttyUSB1"
baud = 9600
# paniolo lab — managed by the `paniolo` CLI; hand-edits are preserved

