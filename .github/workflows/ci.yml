# Copyright 2026 Curtis Galloway
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs on the same ref (e.g. rapid pushes to a PR).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  coverage:
    # Project rule: every crate in the repo has a CI job, and
    # scripts/ci-local.sh mirrors the Linux ones. This job fails the build
    # when a new crate lands without either, so coverage gaps surface at the
    # PR that creates them instead of months later.
    name: ci coverage (every crate has a job)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check every crate has a CI job
        run: bash scripts/ci-coverage-check.sh

  serialcap:
    name: serialcap (fmt, clippy, test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: serialcap
    steps:
      - uses: actions/checkout@v6
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y pkg-config libudev-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: serialcap
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test

  cli:
    name: cli (fmt, clippy, test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cli
    steps:
      - uses: actions/checkout@v6
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y pkg-config libudev-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: cli
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test

  netbootd:
    name: netbootd (fmt, clippy, test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: netbootd
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: netbootd
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test

  hdmicap-linux:
    name: hdmicap (Linux, fmt, clippy, V4L2 build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hdmicap
    steps:
      - uses: actions/checkout@v6
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config libclang-dev clang cmake nasm libturbojpeg0-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: hdmicap
      - name: Format
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Build hdmicap (V4L2 path)
        run: cargo build

  cambrionix:
    name: cambrionix (fmt, clippy, test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cambrionix
    steps:
      - uses: actions/checkout@v6
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y pkg-config libudev-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: cambrionix
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test

  ch9329:
    name: ch9329 (fmt, clippy, test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ch9329
    steps:
      - uses: actions/checkout@v6
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y pkg-config libudev-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ch9329
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test

  hidrig:
    name: hidrig (fmt, clippy, test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hidrig
    steps:
      - uses: actions/checkout@v6
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y pkg-config libudev-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: hidrig
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test

  usbhub:
    name: usbhub (fmt, clippy, test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: usbhub
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: usbhub
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test

  shellyplug:
    name: shellyplug (fmt, clippy, test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: shellyplug
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: shellyplug
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test

  amt:
    name: amt (fmt, clippy, test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: amt
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: amt
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test

  windows:
    # The Windows port. Every crate is built here, because the whole point of
    # the port is that the `#[cfg]` branches in cli/src/platform.rs and its
    # copies only ever compile on the platform they are written for — a Unix-only
    # CI cannot tell you that the Windows arm still builds.
    #
    # This runs the same fmt/clippy/test triple as the Linux jobs, in one job
    # rather than ten, because the crates are small and a Windows runner minute
    # costs twice a Linux one. A per-crate failure still names the crate.
    #
    # Not covered here, and not coverable: hdmicap has no capture backend on
    # Windows and usbhub cannot claim a hub interface, so both compile and test
    # but neither can talk to hardware. See AGENTS.md under **Platform support**.
    name: windows (all crates, fmt, clippy, test)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            cli
            hdmicap
            serialcap
            netbootd
            cambrionix
            hidrig
            ch9329
            usbhub
            shellyplug
            amt
      - name: Format, clippy and test every crate
        shell: pwsh
        run: |
          $crates = "cli","hdmicap","serialcap","netbootd","cambrionix","hidrig","ch9329","usbhub","shellyplug","amt"
          $failed = @()
          foreach ($c in $crates) {
            Write-Host "::group::$c"
            Push-Location $c
            cargo fmt --check;   if ($LASTEXITCODE -ne 0) { $failed += "$c (fmt)" }
            cargo clippy --all-targets -- -D warnings; if ($LASTEXITCODE -ne 0) { $failed += "$c (clippy)" }
            cargo test;          if ($LASTEXITCODE -ne 0) { $failed += "$c (test)" }
            Pop-Location
            Write-Host "::endgroup::"
          }
          if ($failed.Count -gt 0) {
            Write-Host "::error::failed: $($failed -join ', ')"
            exit 1
          }

  macos:
    # Exercises the macOS-only stack the Linux jobs can't: hdmicap's AVFoundation
    # capture path and the Apple Vision OCR helper. fmt/clippy run on both Linux
    # (V4L2 path) and here (AVFoundation path) so each per-OS cfg branch is gated
    # on the platform that compiles it.
    name: macos (hdmicap, visionocr)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: hdmicap
      - name: Format
        working-directory: hdmicap
        run: cargo fmt --check
      - name: Clippy
        working-directory: hdmicap
        run: cargo clippy --all-targets -- -D warnings
      - name: Build + test hdmicap (AVFoundation capture path)
        working-directory: hdmicap
        run: cargo test
      - name: Compile visionocr (Apple Vision OCR helper)
        run: swiftc -O -o "$RUNNER_TEMP/visionocr" ocr/visionocr.swift
