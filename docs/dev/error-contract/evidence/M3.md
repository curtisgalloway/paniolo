<!--
SPDX-FileCopyrightText: 2026 Curtis Galloway
SPDX-License-Identifier: Apache-2.0
-->

# M3 evidence — daemons, timeouts, `serial log`

Plan: [implementation plan](../implementation-plan.md). Design: [design](../design.md).
Starting revision: `ddd8189` (M2 checkpoint); branch `error-contract`.

## Inventory (the milestone's first step)

`rg` over `cli/src` for daemon-down, timeout and device-node sites:

| Kind | Sites |
|---|---|
| daemon_down (100) | `serial send`, `power-state` sense (serialcap); `console`, `video preview`, OCR (hdmicap) — each a `daemon(&target)` lookup that found no discovery record. Plus any daemon request refused at connect (a stale record). |
| timeout (22) | `daemons::start_failure` (4 start paths: restart, `serial watch`, `video watch`, `console`, hid serve), `ssh::forward` port wait, a daemon request that timed out. |
| helper_failed (101) | a daemon answering an error status (`/input`, `/button`, OCR, stable-frame wait); a daemon that exited during startup. |
| device node (4) | **none**: paniolo never opens or stats a device node before a daemon does; the daemons report it, through start failure or an error status. Design table row 4 and the plan's M3 outcome revised. |

hid and netbootd: hid is started on demand (`hid serve`, `console`), so only its
start failure applies; netbootd has its own `netboot status` reporting and no
"needs the daemon" command path.

## Acceptance criteria and result

| Criterion | Result |
|---|---|
| `serial send` with serialcap stopped → 100, `channel: serial`, target set | pass (`a_stopped_serial_daemon_is_daemon_down`) |
| `serial log --require-live` with serialcap stopped → 100; without it a `warning:` line, not 100 | pass (`serial_log_warns_when_the_daemon_is_stopped_and_can_require_it`) |
| refused / timed-out / error-status daemon requests → 100 / 22 / 101 with the daemon's reason, sanitized | pass (unit `error::tests::a_refused_daemon_request_is_daemon_down`, `a_daemon_that_never_answers_is_a_timeout`, `a_daemon_error_status_is_helper_failed_with_its_reason`, `daemon_text_loses_control_characters_and_is_capped`) |
| daemon exited non-zero during startup → 101 with its code; still starting, or a helper that forked and exited 0 → 22 | pass (unit `daemons::tests::start_failure_tells_an_exited_daemon_from_a_slow_one`) |
| missing required arguments are all named in the usage object | pass (`a_missing_required_argument_names_it`) |
| an untracked (swept discovery file) serialcap counts as live for `serial log` | covered by construction: the check is `serial::untracked`, the same one `serial show` uses; staging a fake orphan needs a binary in paniolo's helper dir |

## Verification

From `cli/`, rustup stable (cargo 1.98.1):

```
cargo fmt --check                              # clean
cargo clippy --all-targets -- -D warnings      # clean
cargo test
test result: ok. 221 passed; 0 failed   (unit)
test result: ok. 25 passed; 0 failed    (tests/error_contract.rs)
```

`python3 evals/run.py --check`: `OK — all references and the T1-safe
allowlist match the live CLI`.

## Review

Method: `review-swarm`, seven arms (security, correctness, compat, docs,
history, conventions, perf), over `git diff ddd8189` of the working tree
before the checkpoint commit. The installed checker defaulted to four arms;
it was re-run with `--arms` naming all seven. Mechanical check: 10 verified,
0 dropped. Referee: 10 kept, 0 dropped, 0 merged. perf and conventions
reported none. The run directory was session scratch space; the refereed
table is reproduced here.

**Review swarm: 10 findings (all 7 arms delivered).**

All 7 arms delivered.

| # | ID | Severity | Arms | Location | Claim | Suggested fix |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | F9 | medium | history | `cli/src/main.rs:3326-3334` | The new `serial log` liveness check treats a missing discovery file as a stopped daemon. The history already fixed this mistake elsewhere: b1b8654a915b638ad888609c5fe115d0de7650ca (#195) says "`serial show` then reports `running, untracked (pid N)` instead of `stopped`", and the `serial show` code carries the comment "\"stopped\" has to mean the ports are free. A daemon whose discovery file was deleted under it is still running and still holding them (#187)." AGENTS.md records the cause: the Debian /tmp sweep deletes the discovery file of a daemon that has run ten days while it keeps capturing. For that untracked daemon, `serial log` now prints a false warning ("not running ... nothing captured since it stopped") while the log is still growing, and `--require-live` refuses a live capture with exit 100 daemon_down. The same `.is_none()`-only check now also drives exit 100 in `serial send`/`power state`, but those messages already said "not running" before this change. | When `serial::daemon` is None, check `serial::untracked(&serials)` the way `serial show` does. If an orphan exists, say it is running untracked and point to `p… |
| 2 | F7 | low | docs | `cli/src/error.rs:183` | The doc comment refers to a `secs` deadline, but the function it documents (cli/src/error.rs:185, `pub fn daemon_start_timeout(daemon: &str, message: String) -> PanioloError {`) has no such parameter. The deadline appears only inside the caller-built message, so the doc names an input that does not exist. | Say "within its start deadline" (the caller puts the value in `message`), or add the parameter. |
| 3 | F10 | low | history | `cli/src/error.rs:183-186` | `start_failure` now always returns `timeout` (22, which the design says means the outcome is unknown and the consumer should check state before retrying), with a doc comment saying the daemon "may yet come up". The history shows one common way to reach this path is a replacement that has already exited for good: 3fe3f50cc1844d86d2a205cea663826de2325892 (#194) says the restart failed while "the replacement had actually exited with `another hdmicap daemon is already running`". In that case the daemon will not come up, and retrying hits the same lock again. This is a moderate-confidence classification concern: the message still carries the stderr tail. | In `start_failure`, check whether the spawned daemon's pid has already exited. If it has, classify the failure as helper_failed (or daemon_down) with its stder… |
| 4 | F6 | low | docs | `cli/src/error.rs:190-191` | The doc says only connection refused maps to daemon_down, and design.md:102 says "a daemon request that timed out" is `timeout` (22). The code at cli/src/error.rs:199 (`ureq::ErrorKind::ConnectionFailed => Kind::DaemonDown,`) maps every ConnectionFailed to daemon_down. In ureq 2.12.1 that includes a connect timeout (stream.rs:426 wraps any `TcpStream::connect_timeout` error as `ErrorKind::ConnectionFailed`), so a request whose connect timed out exits 100, where both docs say 22. This is unlikely against a loopback daemon but possible, for example when the listen backlog is full. | Classify ConnectionFailed by its io::ErrorKind source (ConnectionRefused becomes daemon_down, TimedOut becomes timeout), or reword both docs to say that any co… |
| 5 | F3 | low | correctness | `cli/src/error.rs:199` | ureq 2.12.1 reports a connect that hit its deadline as ErrorKind::ConnectionFailed too (stream.rs: `return Err(ErrorKind::ConnectionFailed.msg("Connect error").src(e));` after `TcpStream::connect_timeout`), so a live but wedged daemon whose accept backlog is full is classified daemon_down (100, 'discovery record is stale') rather than timeout (22, outcome unknown). This is unlikely on loopback, so confidence that it matters in practice is moderate. | In the ConnectionFailed arm, check is_timeout(t) first and map a timed-out connect to Kind::Timeout; map only refused/other connect errors to DaemonDown. |
| 6 | F1 | low | security | `cli/src/error.rs:206-209` | daemon_request_failed now copies the full, unsanitized HTTP error body into the error message for serialcap /input and /button, which before this change reported only ureq's status line (OCR already did this). report() prints the message raw in human mode (cli/src/error.rs:357 `eprintln!("{}", e.message);`), so whatever answers on the loopback port can put control or ANSI escape sequences on the user's terminal. The discovery record's port can be stale, and paniolo does not authenticate the server, so a local process squatting that port qualifies. Confidence is moderate: it needs a local attacker, and the JSON path escapes these characters. | Before embedding the body, strip or escape control characters (keep printable text and maybe newlines) and cap its length, for example to the first line or 200… |
| 7 | F5 | low | compat | `cli/src/error.rs:208-209` | daemon_request_failed now rewrites the message for HTTP error statuses from serialcap /input (serial.rs send_input), serialcap /button (power.rs dtr_press_daemon) and the stable-frame wait (video.rs ocr): before M3 these printed ureq's own text (e.g. `serialcap /input failed: http://127.0.0.1:<port>/input: status code 400`), now they print the daemon's response body or `daemon returned status N`. That changes user-visible and eval-asserted message text, which design.md line 193 promises does not change (`Message text does not change, so eval scenarios asserting text keep passing`); no in-repo eval asserts these strings today (grep found none), so the break is to external scripts only. | Accept the change and correct the compatibility note in design.md to list these message changes, or keep the ureq status text for the three sites that previous… |
| 8 | F2 | low | correctness | `cli/src/error.rs:329` | The new usage-message rule keeps only the single line after a message ending in ':', but clap prints one line per missing argument, so when two or more required arguments are missing the JSON message names only the first one and silently drops the rest. The comment just above says the next line 'names them', which is only true for one argument. | Collect all following non-empty lines up to the blank line or the 'Usage:' line and join them with ', ' instead of taking only lines.next(). |
| 9 | F4 | low | correctness | `cli/src/serial.rs:142` | The new classified error from `serial send` (and the matching `serialcap /button` site at cli/src/power.rs:55) never gets .target(), and its caller at cli/src/main.rs:3302 `serial::send_input(&daemon, &ch.name, &payload, pace_ms)?;` propagates it unchanged. The JSON `target` is therefore null even though the target is known. The sibling video::ocr path in the same change does add `.target(target)`, and the design says target is null only when unknown. | Add .target(&target) where the callers in main.rs map the error, or pass the target into send_input/dtr_press_daemon and set it there, as video::ocr does. |
| 10 | F8 | low | docs | `docs/dev/error-contract/implementation-plan.md:72-75` | The M3 outcome promises daemon_down for the hid and netbootd checks and exit 4 for an absent configured device node. The design as changed in this diff says the opposite: design.md:101 says "paniolo itself never probes device nodes, so it does not emit 4 for one", and design.md:103 limits daemon_down to "(serialcap, hdmicap)". The code matches the design: the only hid site is `daemons::start_failure(HID_DAEMON, ...)` at cli/src/main.rs:4349, which yields timeout, and nothing in cli/src constructs Kind::Unreachable for a device node. The plan therefore describes M3 as doing things it does not do. | Update the M3 outcome to match the revised design: no device-node 4, daemon_down for serialcap and hdmicap only, and a hid daemon that fails to start is a time… |

Dropped before this table: checker dropped 0; referee dropped 0; merged 0.

### Resolution record (user: fix all; F10 split by whether the daemon exited)

| ID | Sev | Outcome | Change and covering check |
|---|---|---|---|
| F9 | medium | fixed | `serial log` counts an untracked serialcap (`serial::untracked`) as live, as `serial show` does (#187, #195). Covered by construction (see above). |
| F3 | low | fixed | `ConnectionFailed` or `Io` whose io error is `TimedOut`/`WouldBlock` → `timeout`; other `ConnectionFailed` → `daemon_down`. |
| F6 | low | fixed | Same change; `daemon_request_failed` doc says "a connect or read that timed out". |
| F1 | low | fixed | `printable`: control characters to spaces, cap 300 characters. Unit test. |
| F5 | low | fixed (docs) | New text kept (the daemon's reason is more useful); design compatibility note lists every message change from M2/M3. |
| F2 | low | fixed | All names clap lists are joined. Test with two missing arguments. |
| F4 | low | fixed | `error::with_target` fills `target` for `serial send` and the DTR press. |
| F7 | low | fixed | Doc wording. |
| F8 | low | fixed | Plan's M3 outcome matches the revised design. |
| F10 | low | fixed (user decision) | Start paths keep the spawned `Child`; `start_failure` uses `try_wait()` (a pid probe would see a zombie as alive): exited → `helper_failed` with its code and stderr tail, still running → `timeout`. Unit test. Design rows 22 and 101 updated. |

### Re-review of the fixes

Method: one fresh-context reviewer subagent, given the refereed findings,
both diffs and the user's decisions; quote-backed verdicts. All ten findings
**fixed**. Regression checks it reported clean: holding the spawned `Child`
changes nothing about daemon lifetime (std's `Child` neither kills nor waits
on drop; detach is unchanged; no piped stdio); the
`ConnectionFailed | Io if is_timeout(t)` arm matches ureq 2.12.1 (connect
errors are `ConnectionFailed` wrapping the io error; a connect deadline is
`TimedOut`; a read timeout's `WouldBlock` becomes `TimedOut`); `with_target`
keeps an already-set target; the F9 check costs one `ps` scan, only when the
discovery file is missing.

New findings, all fixed:

| ID | Sev | Finding | Resolution |
|---|---|---|---|
| N1 | low | A helper that forks and exits 0 (allowed by the hid `cmd` contract) would be reported as `helper_failed` "exited with code 0" if discovery was slow. | A successful exit is not treated as exited: it stays `timeout`. Unit test extended. The misplaced hid contract doc comment moved onto `ensure_hid_daemon_local` and says either shape is fine. |
| N2 | nit | A 300-character body plus a newline got a false `…`. | Body trimmed before `printable`. |
| N3 | nit | `with_target`'s by-value downcast would drop `.context()` added above it. | Documented: call it on the error as the lower layer returned it (both callers do). |
| N4 | nit | The F10 unit test relied on a fixed 300 ms sleep. | Polls `try_wait` with a deadline (std caches the status, so `start_failure` still sees it). |

Noted, not changed (pre-existing, outside this milestone's fixes): a
non-timeout `Io` transport error (e.g. connection reset) is still `internal`;
`serial send` / `power-state` report `daemon_down` for an untracked
serialcap, which has no discovery record and so cannot be reached anyway.
