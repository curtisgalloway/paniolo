<!--
SPDX-FileCopyrightText: 2026 Curtis Galloway
SPDX-License-Identifier: Apache-2.0
-->

# M4 evidence — documentation and final verification

Plan: [implementation plan](../implementation-plan.md). Design: [design](../design.md).
Starting revision: `fb5fe65` (M3 checkpoint), clean tree; branch `error-contract`.

## Scope change (user decision, 2026-09-25)

The hardware half of M4 — R6 (`power-cycle <unknown-target>` silent on a
control host) and the consumer's five cases against a control host running
this build — is **moved to M5**, not dropped. No control host with a lab file
was reachable from this session, and the user chose to run the check in the
release train after `apt upgrade`. R6 stays open until then.

## Acceptance criteria and result

| Criterion | Result |
|---|---|
| `paniolo --help` has an exit-status section listing only emitted codes | pass: `EXIT_STATUS_HELP` as clap `after_help` (top level only); unit test `error::tests::help_lists_exactly_the_emitted_codes` pins its code/kind lines to `Kind::ALL` plus 0 and 1. Every `Kind` is constructed somewhere in `cli/src` (grep of constructors); the only `exit(1)` is `doctor` (`main.rs`) |
| A user-facing doc describes the contract and the JSON object | pass: new `docs/errors.md` (in `mkdocs.yml` nav, `docs/README.md` index, a `README.md` capabilities row); cross-links from `serial.md` (`--require-live`), `power.md`, `distributed-control.md` |
| `skills/paniolo/SKILL.md` describes it | pass: "When a command fails — read the exit status" section |
| `AGENTS.md` describes it | pass: `error.rs` in the module map, and a rule for new failures and child statuses |
| Eval scenarios asserting rc 1 updated | none exist: no scenario or grader compares an exit code (`grep` of `evals/`); `run.py` only records `rc` |
| The documented example is real output | pass: `paniolo --json-errors power-cycle nosuch` against a scratch lab printed the two lines in `docs/errors.md` and exited 3 (JSON keys are sorted by serde; the doc says key order is not part of the contract) |
| R6 and the consumer's cases on a control host | **moved to M5** (see above) |

## Verification

From `cli/`, rustup stable, after the review fixes:

```
cargo fmt --check                              -> clean
cargo clippy --all-targets -- -D warnings      -> Finished, no warnings
cargo test                                     -> 222 passed; 25 passed; 0 failed
```

From the repo root:

```
uv run --no-project --python 3.12 python evals/run.py --check
  -> OK — all references and the T1-safe allowlist match the live CLI
uvx --with mkdocs-material mkdocs build --strict  -> built, no WARNING
git diff fb5fe65 | grep '^+' | grep -E '<private host/address/path patterns>'  -> none
```

(`python3.12` is not installed on this machine; uv's 3.12 was used.)

Prove-it-bites, each on a copy of `error.rs` restored afterward (`cmp` clean):

- Deleting the `4 unreachable` line from `EXIT_STATUS_HELP` →
  `help_lists_exactly_the_emitted_codes ... FAILED`.
- Adding a `Kind::Bogus` variant → `error[E0004]: non-exhaustive patterns`.
  Every exhaustive match on `Kind` (`exit_code`, `as_str`, and the new
  `listed_in_all` guard) fires; the guard's value is that one of those errors
  sits beside `Kind::ALL`, not that it adds protection the others lacked.

## Review

Method: `review-swarm`, seven arms, over `git diff fb5fe65` of the working tree
(new `docs/errors.md` marked intent-to-add so it was in the diff), before the
checkpoint commit. Mechanical check: 8 verified, 0 dropped, 1 outside the
diff. Referee: 6 kept, 0 dropped, 2 merged (F1 into F4, F7 into F3). security,
compat and perf reported none. The run directory was session scratch space;
the refereed table is reproduced here. The user chose to fix all six.

**Review swarm: 6 finding(s).**

All 7 arms delivered.

| # | ID | Severity | Arms | Location | Claim | Suggested fix |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | F2 | medium | docs | `docs/errors.md:38` | The documented daemon_down remedy `paniolo daemons restart` cannot work as written: cli/src/main.rs:1288-1289 `if !all && !stale_only && names.is_empty() {` / `bail!("name one or more daemons (serialcap, hdmicap), or pass --all / --stale");` rejects the bare form, and because it is a bare `bail!` it exits 109 `internal`. Even with a name or --all, the restart selects only from `daemons::list_discovered()` (main.rs:1303), so it does not start a daemon that is not running; the same advice is repeated in skills/paniolo/SKILL.md:64 ("or `paniolo daemons restart`"), which agents follow. | Drop `paniolo daemons restart` from the daemon_down remedy (in docs/errors.md and SKILL.md) or give it the arguments that actually work for the stale-record ca… |
| 2 | F6 | medium | history | `docs/errors.md:52` | The doc says `--json-errors` works 'anywhere on the command line', but the M1 review already established it does not: the resolution record of 652c7859060f522b378e4659a383d34ac7ec5062 (docs/dev/error-contract/evidence/M1.md, F4) says 'trailing-arg commands (`hid send`, `adb run`, `helper`) take hyphenated payload without `--`', and dispatch.rs's json_errors_arg comment says argv scanning 'cannot tell an option from a payload token of the same spelling'. Because those subcommands (plus `adb input`) use `trailing_var_arg = true, allow_hyphen_values = true` (cli/src/main.rs:210, 692, 783, 800), a consumer who follows the doc and appends `--json-errors` after the payload gets no JSON object and the flag is handed to the helper/adb as an argument. | Say the flag goes before the subcommand (e.g. `paniolo --json-errors hid send ...`), or anywhere before a trailing payload, and recommend PANIOLO_JSON_ERRORS=1… |
| 3 | F4 | low | correctness, docs | `cli/src/error.rs:21-22` | The doc comment says a test checks the help table against `Kind`, but the test only compares it to a hand-written list: help_lists_exactly_the_emitted_codes builds `let all = [ Kind::Usage, ... Kind::Internal, ];` (error.rs:453-461) and never enumerates the enum. If someone adds a Kind variant and updates exit_code/as_str (which the compiler forces) but not this array or EXIT_STATUS_HELP, the test still passes. AGENTS.md makes the same claim: "`EXIT_STATUS_HELP` (a test pins the two together)". | Make the test exhaustive, for example with a `Kind::ALL` constant built next to the enum or a match with no wildcard arm, or change the comment to say the test… |
| 4 | F3 | low | docs, history | `docs/errors.md:33` | Two commands that exec another program are missing from the passthrough list (docs/errors.md:96-100, and EXIT_STATUS_HELP in cli/src/error.rs:35-36), so the claim that only doctor exits 1 is wrong for them. `serial connect` ends in `serial::exec_tio` -> `crate::platform::exec_replace(` (cli/src/serial.rs:158), and `adb shell` calls `adb::exec_shell` -> `let err = crate::platform::exec_replace(&mut command(&av));` (cli/src/adb.rs:68). Both replace the process, so the exit status is whatever tio or adb returns, including 1. | Add `paniolo serial connect` and `paniolo adb shell` to the passthrough list in docs/errors.md, EXIT_STATUS_HELP and SKILL.md. |
| 5 | F8 | low | conventions | `cli/src/platform.rs:454` (outside diff) | The rule this change adds to AGENTS.md (the 'Errors follow the exit-status contract' paragraph, after the module layout) says: "Never `std::process::exit(status.code()…)` with a child's status outside the documented passthroughs, and use `error::shell_code` there so a signal death is 128+N, not 255." The Windows `exec_replace` does exactly that pattern, with `unwrap_or(1)` instead of `error::shell_code`, and it serves `serial` tio consoles and `adb` (serial.rs:158, adb.rs:68), which are not all in the documented passthrough list in docs/errors.md. So the newly written rule is broken by existing code on the day it is written. On Windows `code()` is always `Some`, so the exit value does not change today; the conflict is between the rule and the code, and it gives a reader a counterexample to copy. | Use `crate::error::shell_code(status)` here, and either list the interactive consoles (`serial connect`/tio, `adb shell`) as passthroughs in docs/errors.md, or… |
| 6 | F5 | low | docs | `docs/errors.md:68` | For a command-line parse failure, `message` is not the text printed above it. report_parse_error prints clap's full text (cli/src/error.rs:367 `let _ = err.print();`, which includes the `error: ` prefix and the Usage block), but builds `message` from the first line only, with the prefix removed (error.rs:386 `let first = first.strip_prefix("error: ").unwrap_or(first);`), or from the fixed string "a subcommand is required". | Say that for `usage` errors from argument parsing, `message` is clap's first error sentence without the `error: ` prefix, not the whole usage text. |

Dropped before this table: checker dropped 0; referee dropped 0; merged 2.

### Resolutions

| ID | Resolution | Covered by |
|---|---|---|
| F2 | Fixed: the `daemon_down` remedy in `docs/errors.md` and `SKILL.md` is now `serial watch` / `video watch` (what the error messages themselves say), with `paniolo daemons` to list; `daemons restart` removed from that advice | read against `main.rs` daemon_down messages and `daemons --help` |
| F6 | Fixed: docs, skill and `--help` say `--json-errors` goes before any trailing arguments and recommend `PANIOLO_JSON_ERRORS=1` for scripts; "anywhere" removed from user docs | M1 evidence F4 (the known limitation) |
| F4 | Fixed: `Kind::ALL` (test-only) with an exhaustive-match guard beside it; both tests use it; comments in `error.rs` and `AGENTS.md` say exactly what it does and does not force | prove-it-bites above |
| F3 | Fixed: `serial connect` and `adb shell` added to the passthrough list in `docs/errors.md`, `EXIT_STATUS_HELP` and `SKILL.md`; exit-1 row now says a passthrough may exit 1 for its program | `serial connect`/`adb shell` reach `platform::exec_replace` (`serial.rs`, `adb.rs`) |
| F8 | Fixed: Windows `exec_replace` uses `error::shell_code`. Same value on Windows (`code()` is always `Some` there; `shell_code` falls back to 1 like before) | not compiled here (`#[cfg(windows)]`); the Windows CI job builds it |
| F5 | Fixed: the `message` row says a `usage` parse error carries only clap's first sentence without `error: ` | read against `report_parse_error` |

No finding was left unfixed.

## Limitations carried forward

- R6 and the consumer's hardware cases: M5.
- The F8 change is not compiled on this machine (`#[cfg(windows)]`); the
  Windows CI job is its first build.
