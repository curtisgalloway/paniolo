<!--
SPDX-FileCopyrightText: 2026 Curtis Galloway
SPDX-License-Identifier: Apache-2.0
-->

# M1 evidence — error type and the `main()` mapping

Plan: [implementation plan](../implementation-plan.md). Design: [design](../design.md).
Starting revision: `65e38ea` (plan commit); branch `error-contract`.

## Milestone as planned

**Outcome:** `paniolo --lab lab.toml power-cycle nosuch` exits **3**; with
`PANIOLO_JSON_ERRORS=1` or `--json-errors`, the last stderr line is the JSON
error object. Unclassified errors exit **109**. Clap errors exit 2. `doctor`
still exits 1 for problems found.
**Design coverage:** R1 (usage, not_configured, internal), R2, R6 (local), R7.

**Acceptance criteria and result**

| Criterion | Result |
|---|---|
| Unknown target → 3, JSON `target` set | pass (`unknown_target_is_not_configured`: `power-cycle`, `serial log`) |
| Target without a channel → 3, JSON `channel` set | pass (`missing_channel_is_not_configured`: hid, power) |
| Unknown serial interface → 3 | pass (`missing_serial_interface_is_not_configured`, `unknown_dtr_interface_is_not_configured`) |
| Missing lab file → 3 | pass (`missing_lab_file_is_not_configured`) |
| Ambiguous target / interface host → 2 `usage` | pass (`ambiguous_target_is_usage`, `serial_on_two_hosts_without_an_interface_is_usage`) |
| Clap errors → 2, JSON when requested, none for `--help` | pass (`clap_usage_errors_exit_2_with_a_usage_object`) |
| Unclassified → 109 `internal` | pass (`unclassified_error_is_internal`; M3 moves this example to 100) |
| Without opt-in, stderr is exactly the 0.4 text; flag ≡ variable; stdout clean | pass (`no_json_unless_requested_and_the_flag_matches_the_variable`) |
| `--json-errors` crosses dispatch when requested; a repeat is accepted | pass (unit `dispatch::tests::json_errors_crosses_the_hop_when_requested`, `a_repeated_json_errors_flag_is_accepted`) |
| `doctor` still exits 1 | unchanged code path (`cmd_doctor`), not re-tested |
| R6 silent power-cycle | **not reproduced locally** with 0.4.1 or this build (prints and exits 3); the control-host repro is M4 |

## Verification

Toolchain: rustup stable (cargo 1.98.1), from `cli/`:

```
cargo fmt --check                              # clean
cargo clippy --all-targets -- -D warnings      # clean
cargo test
test result: ok. 214 passed; 0 failed   (unit)
test result: ok. 14 passed; 0 failed    (tests/error_contract.rs)
```

From the repo root, `python3 evals/run.py --check`:
`OK — all references and the T1-safe allowlist match the live CLI`.

Manual probe (this build, eval fixture lab):

```
$ paniolo --json-errors --lab lab.toml power-cycle nosuch
target 'nosuch' not found in lab
{"error":{"channel":null,"child_exit":null,"code":3,"host":null,"kind":"not_configured","message":"target 'nosuch' not found in lab","target":"nosuch"}}
rc=3
```

## Review

Method: `review-swarm` (four arms: security, correctness, compat, docs;
mechanical quote check; referee), over `git diff 65e38ea` of the working
tree before the checkpoint commit. The mechanical check verified 10 findings
and dropped 0; the referee kept 9, merged 1 (F1 into F5), dropped 0. The run
directory was session scratch space and is not preserved; the refereed table
is reproduced here.

**Review swarm: 9 findings (all 4 arms delivered).**

All 4 arms delivered.

| # | ID | Severity | Arms | Location | Claim | Suggested fix |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | F5 | medium | compat, correctness | `cli/src/dispatch.rs:197` | When PANIOLO_JSON_ERRORS=1 is set (without the flag on the command line), dispatch() now appends `--json-errors` to the remote paniolo argv. A 0.4.x control host has no such flag (the 65e38ea Cli has no json_errors field), so its clap parser rejects it with a usage error (exit 2) and the dispatched command, e.g. power-cycle, never runs. This is worse than the skew D5 accepts ('A 0.4.x remote paniolo exits 1 with no JSON'); the plan's FORWARDED_ENV route would have been ignored by an old remote. | Carry the request in a form an old remote ignores, such as the PANIOLO_JSON_ERRORS variable through ssh::FORWARDED_ENV as design D1 and plan step 2 specify, or… |
| 2 | F6 | medium | docs | `cli/src/error.rs:14-15` | The doc says dispatch passes `--json-errors` to the remote paniolo whenever JSON errors are requested, but only `dispatch()` does so. `dispatch_stdout_to_file` (cli/src/dispatch.rs:295-296: `let mut argv = vec![host.paniolo(), "--lab".to_string(), remote_path.clone()];` / `argv.extend(sub_argv.iter().cloned());`), used by remote `video shot` and `adb screencap` with an argv built in main.rs, and `run_subcommand` (dispatch.rs:235) never add the flag. The variable does not cross either, because cli/src/ssh.rs:129 is `pub const FORWARDED_ENV: &[&str] = &["AMT_PASSWORD"];`. So `--json-errors video shot` against a remote target prints no JSON object when the remote command fails. | Apply `json_errors_arg` in `dispatch_stdout_to_file` (and `run_subcommand`), or narrow the doc to name `dispatch()` only. |
| 3 | F4 | low | correctness | `cli/src/dispatch.rs:220` | The duplicate check scans the whole remote argv, including positional payload after `--` or free-form trailing args (e.g. `hid send` args), so a payload token equal to `--json-errors` suppresses the flag and the remote prints no JSON object even though JSON was requested. Low likelihood; the check should only consider option position before the subcommand's positionals. | Stop scanning at the first `--`, or always prepend the flag (clap `SetTrue` accepts a repeated global bool flag only if configured; alternatively strip it from… |
| 4 | F2 | low | correctness | `cli/src/dispatch.rs:348` | Every `channel_host` error is relabeled as a missing channel, but `channel_host` (model.rs:575-580) also returns `"target '{}' has serial interfaces on multiple hosts ({}); specify one with --interface"`, which is an ambiguity the caller resolves by naming an interface. The change classifies the analogous `"Multiple targets ({}) — specify one with -t."` in main.rs `resolve_single_target` as `Kind::Usage` (exit 2), so the two ambiguity cases now exit with different codes (3 vs 2). | Classify the multiple-hosts ambiguity the same way as the multiple-targets one (usage), or make both not_configured, so equivalent failures share one code. |
| 5 | F7 | low | docs | `cli/src/error.rs:23-24` | The `NotConfigured` doc says an unknown interface is `not_configured` (exit 3). The unknown-interface lookups in main.rs still return a bare `anyhow!`, which `classify` maps to `internal` (109). One is `resolve_dtr_interface` at main.rs:2360: `"no serial interface '{name}' on '{target}' (have: {})",`. It runs in `cmd_serial_dtr` before any `maybe_dispatch` validation, so `serial dtr -i <typo>` exits 109. Another is `pick_serial` at main.rs:3073: `.ok_or_else(\|\| anyhow!("no serial interface '{n}' (have: {})", have())),`. | Wrap those interface lookups in `error::channel_missing(target, "serial", ...)`, or leave the doc as is until they are classified and list them as known `inter… |
| 6 | F10 | low | docs | `cli/src/error.rs:126-127` | `channel_missing`'s doc says it covers a target lacking a channel or a channel lacking a field, but it is also used for a channel that exists on another host (usb at main.rs:4075, hid at main.rs:4264), so the doc does not describe the helper's actual use. | Either widen the doc to include "channel not on this host" and classify all such sites the same way, or keep the usb/hid sites as unclassified to match the oth… |
| 7 | F8 | low | docs | `cli/src/main.rs:73-74` | The `--json-errors` help promises a JSON object on any failure. Clap usage failures exit 2 from inside `Cli::parse()` (main.rs:894: `let cli = Cli::parse();`), before the flag is acted on at main.rs:899, and never reach `error::report`. So a bad flag produces no JSON object even with `--json-errors` or PANIOLO_JSON_ERRORS=1. The test `clap_usage_errors_still_exit_2` checks only the exit code. | Say in the help that argument-parsing errors (exit 2) carry no JSON object, or route clap errors through `error::report` with `Kind::Usage`. |
| 8 | F3 | low | correctness | `cli/src/main.rs:2847` | The same 'channel is configured on another host' condition now exits with two different kinds depending on the channel: usb and hid (main.rs:4071-4077 `format!("usb channel for '{target}' is not on this host")` wrapped in `error::channel_missing`, and the hid equivalent) exit 3 `not_configured`, while power, video (3301), netboot (3754) and adb (4309) still `bail!` and so exit 109 `internal`. A consumer branching on the kind gets inconsistent results for one failure class. | Convert the remaining four `is not on this host` sites to `error::channel_missing(..)` like the usb and hid ones (or revert those two), so the condition has on… |
| 9 | F9 | low | docs | `docs/dev/error-contract/design.md:150-151` | The design (and implementation-plan.md M1 step 2, "add `PANIOLO_JSON_ERRORS` to `ssh::FORWARDED_ENV`") says the variable is added to `FORWARDED_ENV`. The change instead passes an argument (dispatch.rs `json_errors_arg`) and leaves cli/src/ssh.rs:129 as `pub const FORWARDED_ENV: &[&str] = &["AMT_PASSWORD"];`. The deviation is explained only in a code comment, so the approved design now describes a mechanism the code does not have. | Update D1 in design.md and step 2 of the plan to record the argument-based forwarding and why it was chosen over FORWARDED_ENV. |

Dropped before this table: checker dropped 0; referee dropped 0; merged 1.

### Resolution record

| ID | Sev | Outcome | Change and covering check |
|---|---|---|---|
| F5 | medium | **accepted, not fixed** (user decision) | Old 0.4.x remote rejects `--json-errors` with exit 2. Kept because the variable route breaks Windows hosts and interactive dispatch. Recorded as design D5. |
| F6 | medium | fixed | `dispatch::remote_argv` used by `dispatch` and `dispatch_stdout_to_file`; `run_subcommand` documented as not passing it (captured, reported locally). |
| F4 | low | fixed (second pass) | First pass stopped the scan at `--`; the re-review showed trailing-arg commands (`hid send`, `adb run`, `helper`) take hyphenated payload without `--`. Final: the global flag overrides itself (repeat accepted) and dispatch always adds it, with no scan. Test `a_repeated_json_errors_flag_is_accepted`. |
| F2 | low | fixed | `model::channel_host` returns `PanioloError`: multi-host ambiguity → `usage`, unknown interface → `not_configured` with target/channel; test `serial_on_two_hosts_without_an_interface_is_usage`. |
| F7 | low | fixed | `resolve_dtr_interface` and `pick_serial` (now takes `target`) raise classified errors; test `unknown_dtr_interface_is_not_configured`. |
| F3 | low | fixed | power, video, netboot, adb "is not on this host" → `channel_missing` like usb/hid. No integration test (needs a local run against a remote-host channel); covered by construction. |
| F10 | low | fixed | `channel_missing` doc lists all four uses. |
| F8 | low | fixed | `main()` uses `try_parse_from`; `error::report_parse_error` prints clap's text, then the usage object when requested (flag scanned from raw argv); none for `--help`/`--version`. Test extended. |
| F9 | low | fixed | design D1/D5 and plan step 2 describe the argument route. |

### Re-review of the fixes

Method: one fresh-context reviewer subagent, given the refereed findings, the
pre-fix and post-fix diffs, and the user's F5 decision; quote-backed verdicts.

| ID | Verdict |
|---|---|
| F2, F3, F6, F7, F9, F10 | fixed |
| F8 | fixed; edge case N3 below |
| F4 | partly fixed → fixed in a second pass (see the F4 row above) |

Regression checks it reported clean: every `channel_host` caller (six `?`,
one `.ok()`); all three `pick_serial` callers; `--help`/`--version`/subcommand
help still exit 0 with no object; no path prints two JSON objects.

New findings (all low), all fixed:

| ID | Finding | Fix and check |
|---|---|---|
| N1 | `serial watch` / daemon start with no serial interfaces exited 109, while the same message via `pick_serial` exited 3 | both → `channel_missing(serial)`; test `serial_watch_without_interfaces_is_not_configured` |
| N2 | ambiguous DTR power-button interface exited 109; other ambiguities are `usage` | → `Kind::Usage` with target/channel; test `ambiguous_dtr_interface_is_usage` |
| N3 | a command group without its subcommand gave a JSON `message` equal to the group's help line | fixed message `a subcommand is required`; test `missing_subcommand_is_usage_with_a_plain_message` |

Known limit, not fixed: on a *parse failure*, `report_parse_error` decides
whether JSON was requested by scanning raw argv for `--json-errors` before
`--`, so a payload token of that spelling on a command that already failed to
parse can add the usage object. Harmless (the exit code is 2 either way) and
unavoidable without a successful parse.
