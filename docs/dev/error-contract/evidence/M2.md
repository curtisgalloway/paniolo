<!--
SPDX-FileCopyrightText: 2026 Curtis Galloway
SPDX-License-Identifier: Apache-2.0
-->

# M2 evidence — the child boundary

Plan: [implementation plan](../implementation-plan.md). Design: [design](../design.md).
Starting revision: `652c785` (M1 checkpoint); branch `error-contract`.

## Milestone as planned

**Outcome:** a failing hook exits 101 with its code in `child_exit`; a
missing hook exits 3; a dispatch whose ssh exits 255 exits 4 with `host`
set; a remote failure arrives with its own code and exactly one JSON object;
a passthrough child killed by a signal exits 128+N, never 255.
**Design coverage:** R1 (unreachable, helper_failed), R3, R4.

**What changed**

- `error.rs`: `shell_code` (code, or 128+N for a signal), `hook_failed`
  (126/127 → `not_configured`, else `helper_failed`, `child_exit` = the code
  or null for a signal), `helper_missing`, `unreachable_host`,
  `SSH_TRANSPORT_FAILURE`.
- `ssh.rs`, `adb.rs`, `video.rs`, `serial.rs`: every `code().unwrap_or(-1|1)`
  is `shell_code`, so a signal-killed child is 128+N, never 255.
- Hooks → `hook_failed`: power `cycle/on/off_cmd`, `state_cmd`, `usb` helper,
  `hid send`, `hid stop`, `serialcap log`, `serialcap stop`, `hdmicap stop`.
- Dispatch: `ship_slice` classifies the SFTP copy (missing sftp → 3, other
  failure → 4 with `host`); ssh 255 → 4 in `dispatch`, `run_subcommand`,
  `dispatch_stdout_to_file`, `setup --host` and remote `adb devices`; the
  slice is removed before the 255 check.
- Passthroughs (design D3) keep the child's code: `config edit`, `helper`,
  `video` and `adb` passthroughs. A missing editor, bundled helper
  (serialcap, hdmicap, netbootd, tio) or adb binary is `not_configured`.
- `daemons stop` / `daemons restart` failures exit 109 instead of 1.
- Docs contradicted by the change fixed now: `docs/power.md` (power-cycle
  exit status) and `docs/dev/adding-power-helpers.md` (hook contract).

**Acceptance criteria and result**

| Criterion | Result |
|---|---|
| `cycle_cmd` `exit 7` → 101, `child_exit: 7`, target and channel set; `exit 2` → 101 | pass (`a_failing_power_hook_is_helper_failed_with_its_code`) |
| missing hook → 3, `child_exit: 127` | pass (`a_missing_power_hook_is_not_configured`) |
| hook killed by a signal → 101, `child_exit: null` | pass (`a_hook_killed_by_a_signal_is_helper_failed_without_a_code`) |
| ssh 255 → 4 with `host`/`target`; slice still removed | pass (`ssh_transport_failure_is_unreachable_not_255`, stub `ssh`/`sftp` on PATH) |
| SFTP copy failure → 4; sftp missing → 3 | pass (`a_failed_slice_copy_is_unreachable`, `a_missing_sftp_is_not_configured`) |
| remote rc 3 + JSON arrives as 3 with exactly one object; `--json-errors` crossed the hop | pass (`a_remote_failure_arrives_with_its_code_and_one_json_object`) |
| passthrough killed by SIGTERM → 143; its own code kept | pass (`a_passthrough_killed_by_a_signal_exits_128_plus_n`) |
| `shell_code` / `hook_failed` classification incl. a real code ≥ 128 | pass (unit `error::tests`) |
| hooks do not inherit the JSON request (one object per failure) | pass (`hooks_do_not_inherit_the_json_request`; proven to fail with the `remove_var` taken out: `left: 9, right: 7`) |
| no `exit(status.code…)` outside passthroughs | pass: `grep 'code().unwrap_or\|exit(status'` finds only the Windows `exec_replace` passthrough and a `doctor` probe value |

Not covered by an integration test (need a real remote or device): `usb`,
`hid send/stop`, `serialcap/hdmicap stop`, `state_cmd`; each uses the same
`hook_failed` path the power tests exercise.

## Verification

From `cli/`, rustup stable (cargo 1.98.1):

```
cargo fmt --check                              # clean
cargo clippy --all-targets -- -D warnings      # clean
cargo test
test result: ok. 216 passed; 0 failed   (unit)
test result: ok. 23 passed; 0 failed    (tests/error_contract.rs)
```

`python3 evals/run.py --check`: `OK — all references and the T1-safe
allowlist match the live CLI`.

## Review

Method: `review-swarm` over `git diff 652c785` of the working tree before the
checkpoint commit. Mechanical check: 10 verified, 0 dropped. Referee: 8 kept,
2 merged (F5 into F1, F7 into F2), 0 dropped; F3 lowered to low. Run
directory was session scratch space; the refereed table is reproduced here.

**Review swarm: 8 findings (all 4 arms delivered).**

All 4 arms delivered.

| # | ID | Severity | Arms | Location | Claim | Suggested fix |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | F2 | medium | correctness, docs | `cli/src/dispatch.rs:222-224` | The NotFound branch meant to classify a missing sftp binary as not_configured cannot fire for that case: ship_slice (dispatch.rs:177-178, `sent.map_err(\|e\| { std::io::Error::other(format!("failed to ship lab slice to {}: {e}", host.ssh))`) rewraps every sftp_put error, including the spawn NotFound from sftp_batch, as ErrorKind::Other. So a host without sftp installed exits 4 `unreachable` ('cannot reach control host') instead of 3. Conversely, the only NotFound that does reach this branch is a local one from `tempfile::NamedTempFile::new()?` (line 173), which is then misreported as 'sftp not found'. | Preserve the io::ErrorKind when ship_slice wraps the sftp_put error (e.g. `std::io::Error::new(e.kind(), ...)`), and distinguish the local temp-file errors fro… |
| 2 | F1 | low | security, compat | `cli/src/dispatch.rs:205-208` | transport_checked now returns early with `?` before the best-effort `ssh::sftp_rm`, so whenever the remote side exits 255 the shipped lab slice (`.paniolo-lab-<pid>-<nanos>.toml` in the control host's home directory, holding the target's hook command lines, device paths and host IPs) is never removed. Before this change cleanup was always attempted. This is a concrete consequence of the documented 255 ambiguity: when a remote passthrough child legitimately exits 255 (or ssh drops after the command ran), the transport is actually up and sftp_rm would succeed, but the slice is stranded anyway. The same early return is in run_subcommand (lines 289-301, before `let _ = ssh::sftp_rm(&host, &remote_path);` at 302). Impact is limited because docs/ssh.rs say credentials travel in forwarded env (`FORWARDED_ENV` = AMT_PASSWORD), not the lab file; it only matters if a lab embeds a secret in a hook command string. Confidence moderate on the security weight, high on the behavior. | Attempt `let _ = ssh::sftp_rm(&host, &remote_path);` before calling transport_checked (it is best-effort and fails fast if the host is really down), in dispatc… |
| 3 | F8 | low | docs | `cli/src/dispatch.rs:183-184` | The doc on `dispatch` says it returns the remote exit code and cleans up the slice afterward. Line 205, `let code = transport_checked(code, host_name, target)?;`, now turns ssh's 255 into an `unreachable` error and returns early, before the `sftp_rm` cleanup at line 208. So on a transport failure, dispatch neither returns the code nor cleans up. | Say that ssh's 255 is reported as `unreachable`, and that cleanup is skipped when the transport is down, as the comment in `dispatch_stdout_to_file` already do… |
| 4 | F3 | low | correctness | `cli/src/dispatch.rs:240-241` | OpenSSH's client exits 255 not only on a transport failure but also when the remote command is terminated by a signal (the server sends exit-signal, not exit-status). So a remote paniolo, or a remote passthrough child, killed by a signal (OOM kill, SIGTERM, a remote Ctrl-C in an interactive dispatch) is reported locally as `unreachable` with 'cannot reach control host ... (ssh exited 255)', exit 4. That contradicts D3's 'a signal-killed child exits 128+N' across the hop, and it is broader than the documented ambiguity, which covers only a child that itself exits 255. Confidence: medium; this rests on OpenSSH client behavior, not on code in this repo. | At minimum, extend the doc comment and the message to say the outcome is unknown (a transport failure or a remote signal death). If a distinction is wanted, ch… |
| 5 | F6 | low | compat | `cli/src/dispatch.rs:288-289` | run_subcommand now applies `?` to ssh::run and returns on 255, both before the `sftp_rm` at line 302 (`let _ = ssh::sftp_rm(&host, &remote_path);`). Before this change the Result was held and `out?` ran only after cleanup, so an ssh spawn or wait I/O error, or a 255 from a transport that recovers, now leaves the shipped lab slice file on the control host. | Hold the ssh::run Result, run `sftp_rm` next, and only then apply `?` and the 255 check, as the pre-change code did. |
| 6 | F4 | low | correctness | `cli/src/error.rs:169` | hook_failed_code nulls child_exit for any code >= 128 because its callers (serial::stop_daemon, video stop) have already collapsed the ExitStatus into an i32 via shell_code, so it cannot tell a signal death from a helper that exited normally with 128 or more, and that real code is lost. hook_failed, which gets the ExitStatus, does not have this problem, so the two helpers apply different rules to the child_exit field. | Pick one rule for both helpers: either always report the numeric shell code in child_exit, or have both helpers null it for 128+N. Then document it in the JSON… |
| 7 | F10 | low | docs | `docs/dev/adding-power-helpers.md:26` | The hook contract table says a non-zero hook exit code is propagated, and the `off_cmd` and `cycle_cmd` rows say "same". The code no longer propagates it: `run_power_hook` (cli/src/main.rs:2948-2957) maps it to `helper_failed` (exit 101) or `not_configured` (exit 3), and the hook's code appears only in the JSON `child_exit` field. | In M4, change the contract column to say that a non-zero exit is reported as helper_failed (101) with the code in `child_exit`, and that 126/127 is reported as… |
| 8 | F9 | low | docs | `docs/power.md:218` | The user doc says power-cycle exits with the hook's return code. After this change, `run_power_hook` (cli/src/main.rs:2948) returns `Err(error::hook_failed(status, ...))`, so a hook that exits 7 makes paniolo exit 101 with `child_exit: 7`, and a hook that exits 126 or 127 makes it exit 3. The test `a_failing_power_hook_is_helper_failed_with_its_code` asserts `Some(101)`. The doc is contradicted, not just missing, but M4 is scheduled to rewrite the docs. | In M4, replace this with: a non-zero hook exits 101 (helper_failed, with the hook's code in `child_exit`), or 3 when the hook is missing or not executable (126… |

Dropped before this table: checker dropped 0; referee dropped 0; merged 2.

### Resolution record (user: fix all eight)

| ID | Sev | Outcome | Change and covering check |
|---|---|---|---|
| F2 | medium | fixed | `ship_slice` classifies its own errors: sftp spawn NotFound → `not_configured`, other SFTP failure → `unreachable`, local temp-file errors unclassified with context. Test `a_missing_sftp_is_not_configured`. |
| F1 | low | fixed | `dispatch` removes the slice before `transport_checked`. Test asserts the `rm` reached sftp after a 255. |
| F6 | low | fixed | `run_subcommand` holds the ssh result, cleans up, then checks. |
| F8 | low | fixed | `dispatch` doc states the 255 → `unreachable` rule and cleanup-either-way. |
| F3 | low | fixed (message/doc) | Exit 4 kept (approved table). Messages now say "the host is unreachable or the remote command was killed; its outcome is unknown", and `transport_checked` documents OpenSSH's signal case. |
| F4 | low | fixed | `serial::stop_daemon` / `video::stop_daemon` return `ExitStatus`; `hook_failed_code` removed, one rule for `child_exit`. Unit test for a real code 200. |
| F10 | low | fixed | `docs/dev/adding-power-helpers.md` hook contract. |
| F9 | low | fixed | `docs/power.md` power-cycle exit status. |

### Re-review of the fixes

Method: one fresh-context reviewer subagent, given the refereed findings,
both diffs and the user's decisions; quote-backed verdicts.

| ID | Verdict |
|---|---|
| F1, F2, F4, F6, F8, F9 | fixed |
| F3 | fixed at the main site; wording missing at three others → N3 |
| F10 | table fixed; stale code comments remained → N4 |

Regression checks it reported clean: the three `ship_slice` callers; all
`stop_daemon` callers; remote non-255 codes still pass through unchanged; no
dispatch path prints two objects.

New findings, all resolved:

| ID | Sev | Finding | Resolution |
|---|---|---|---|
| N1 | medium | A power hook that runs paniolo inherited `PANIOLO_JSON_ERRORS`, so a failing nested call printed its own object ahead of the outer `helper_failed` one (two objects; R2 says one). Introduced by M2 making hooks report errors. | **User decision:** strip it. `error::take_json_request` records the request at startup and removes the variable, so no hook, helper or daemon inherits it; design D1 updated. Test `hooks_do_not_inherit_the_json_request`, proven to bite. |
| N2 | low | A local ssh control-dir failure or a host that refuses the write is also reported as `unreachable` from the slice copy. | Documented on `ship_slice`: exit 4 there means "the copy failed", with sftp's reason in the message. Not reclassified (rare, and the message says why). |
| N3 | low | The "outcome unknown" wording was on one 255 site only; design table row stale. | One `error::ssh_255_message` used by all four sites; design table row 4 updated. |
| N4 | low | Stale "propagating its exit code" comments on the power, usb and hid runners. | Rewritten. |

Pre-existing, not a regression, left as is: a spawn/wait error from ssh (or
`forwarded_env()`) in `dispatch`/`run_subcommand` still returns before the
slice cleanup, as it did at `652c785`.
